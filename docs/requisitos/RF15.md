# RF15 - Notificaciones internas

## Estado del requisito

Cerrado y validado en entorno local.

## Objetivo

Generar avisos internos para los voluntarios suscritos cuando una entidad seguida publica o modifica una actividad, y permitir que esos avisos se consulten y marquen como leidos.

## Alcance funcional

- Existe un modelo de notificacion interna persistido en Prisma.
- Al publicar un evento se generan notificaciones para los voluntarios suscritos a la entidad.
- Al modificar un evento tambien se generan notificaciones.
- El voluntario dispone de un listado propio de notificaciones en `GET /notificaciones`.
- Cada notificacion puede marcarse como leida mediante `POST /notificaciones/:notificationId/leida`.

## Implementacion realizada

### 1. Modelo y migracion

- Se ha definido el modelo `InternalNotification` en Prisma.
- La notificacion queda vinculada a:
  - voluntario destinatario
  - entidad emisora
  - evento relacionado cuando aplica
- Se guarda tipo, titulo, mensaje, estado de lectura y fecha de creacion.

### 2. Generacion de notificaciones

- Se ha creado `src/services/notifications/create-event-notifications.service.js`.
- El servicio recupera la lista de voluntarios suscritos a la entidad.
- Cuando una entidad publica un evento se generan notificaciones con tipo `EVENT_PUBLISHED`.
- Cuando una entidad modifica un evento se generan notificaciones con tipo `EVENT_UPDATED`.
- Si la entidad no tiene suscriptores, no se crean registros.

### 3. Integracion con eventos

- `src/services/events/create-event.service.js` genera avisos despues de publicar el evento.
- `src/services/events/update-owned-event.service.js` genera avisos despues de editar el evento propio.
- El flujo reutiliza la relacion de suscripcion creada en `RF11`.

### 4. Consulta y lectura

- Se ha creado `src/services/notifications/list-volunteer-notifications.service.js`.
- Se ha creado `src/services/notifications/mark-notification-as-read.service.js`.
- Se ha creado `src/controllers/notifications/volunteer-notification.controller.js`.
- Se ha creado la ruta protegida `GET /notificaciones`.
- Se ha creado la ruta protegida `POST /notificaciones/:notificationId/leida`.
- Ambas rutas exigen sesion activa y rol `VOLUNTARIO`.

### 5. Vista e integracion en la interfaz

- Se ha creado `src/views/pages/notifications/index.ejs`.
- La cabecera del sitio incorpora el acceso `Notificaciones` para usuarios con rol `VOLUNTARIO`.
- La pantalla muestra:
  - titulo y mensaje del aviso
  - entidad emisora
  - fecha de generacion
  - enlace al evento relacionado cuando existe
  - accion para marcar como leida la notificacion

### 6. Tests unitarios

- Se ha anadido test unitario del servicio de generacion de notificaciones.
- Se ha anadido test unitario del listado ordenado de notificaciones.
- Se ha anadido test unitario del marcado como leida.
- Se han ampliado los tests de publicacion y edicion de eventos para comprobar que disparan la generacion de avisos.

## Migraciones y base de datos

- `RF15` requiere una migracion nueva de Prisma para crear `InternalNotification`.
- La migracion aplicada para este requisito es `20260422104144_add_internal_notifications_rf15`.

## Validacion realizada

- Ejecutar `npm run prisma:validate`.
- Ejecutar `npm test`.
- Ejecutar `npx prisma migrate dev --name add_internal_notifications_rf15`.
- Ejecutar `npx prisma migrate status`.
- Comprobar en entorno local que un voluntario suscrito ve los avisos en `/notificaciones`.
- Comprobar que al marcar una notificacion como leida el estado se actualiza correctamente en base de datos.

## Comprobacion manual

1. Ejecutar `npm run dev`.
2. Iniciar sesion con una cuenta de voluntario y suscribirse a una entidad.
3. Iniciar sesion con la entidad seguida y publicar un evento nuevo.
4. Volver a entrar con el voluntario y abrir `http://localhost:3000/notificaciones`.
5. Verificar que aparece el aviso de nueva actividad.
6. Editar el evento desde la cuenta de entidad.
7. Volver a revisar el listado y comprobar el aviso de actividad actualizada.
8. Pulsar `Marcar como leida` y confirmar el cambio en pantalla.

## Evidencia de cierre

- El requisito queda implementado en esquema, servicios, rutas, controlador, interfaz y tests unitarios.
- La migracion `20260422104144_add_internal_notifications_rf15` ha quedado creada y aplicada correctamente.
- `npm run prisma:validate` ejecutado correctamente.
- `npm test` ejecutado correctamente.
- `npx prisma migrate status` confirma `Database schema is up to date!`.
- Flujo HTTP real validado en local:
  - login de entidad temporal `302`
  - publicacion de evento `302`
  - edicion de evento `302`
  - acceso de voluntario a `/notificaciones` con `200`
  - marcado de notificacion como leida con `302`
- Verificado en PostgreSQL que las notificaciones se generan y que `isRead` pasa a `true` al marcar como leida.

## Trazabilidad con tareas

- `RF15-01` completada: modelo de notificacion interna definido en Prisma.
- `RF15-02` completada: migracion preparada para crear las notificaciones.
- `RF15-03` completada: logica de notificaciones al publicar un evento implementada.
- `RF15-04` completada: logica de notificaciones al modificar un evento implementada.
- `RF15-05` completada: lista de voluntarios suscritos obtenida desde la base de datos.
- `RF15-06` completada: notificaciones guardadas en base de datos.
- `RF15-07` completada: vista de listado de notificaciones creada.
- `RF15-08` completada: ruta de consulta de notificaciones creada.
- `RF15-09` completada: opcion para marcar una notificacion como leida anadida.
- `RF15-10` completada: tests unitarios de generacion y lectura anadidos.
- `RF15-11` completada: requisito documentado en este fichero.
