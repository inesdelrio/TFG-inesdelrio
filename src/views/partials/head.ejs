<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle && pageTitle !== "Inicio" ? `${pageTitle} | VolunRed` : "VolunRed — Conectando personas con ganas de ayudar" %></title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
